// Invitation acceptance page; real invitation verification will be handled by the backend.
import AuthForm from "@/components/auth/AuthForm";
export default function SignupPage() {
  return <AuthForm signup />;
}
