// Sign-in page matching the invite-only reference. AuthForm submits to the password sign-in API.
import AuthForm from "@/components/auth/AuthForm";
export default function LoginPage() {
  return <AuthForm />;
}
